{
  "name": "pokeapi",
  "version": "1.0.0",
  "description": "a wrapper api to support calling pokedex",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd PokeDex && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eugene Vedensky",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "node-fetch": "^2.3.0"
  }
}
